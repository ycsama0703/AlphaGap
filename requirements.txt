# HTTP & parsing
requests>=2.31
feedparser>=6.0          # arXiv RSS
beautifulsoup4>=4.12     # HF Daily / SSRN scraping fallback
lxml>=5.0

# Config
pyyaml>=6.0
python-dotenv>=1.0

# LLM (DeepSeek API is OpenAI-compatible)
openai>=1.40

# Email
resend>=2.0              # 主推；也可换 smtplib (stdlib)

# Templating
jinja2>=3.1

# CLI / logging
click>=8.1
rich>=13.7               # 漂亮日志

# Testing
pytest>=8.0
pytest-mock>=3.12

# Mechanism evidence library / conference backfill
openreview-py>=2.2
numpy>=1.26
sentence-transformers>=3.0

# Benchmark scorer audit (official TAT-QA scorer imports both)
pandas>=2.0
scipy>=1.11

# Optional (后续阶段)
# tenacity                # 重试装饰器
pypdf>=4.0
